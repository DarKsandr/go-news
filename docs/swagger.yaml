basePath: /api
definitions:
  pkg.CategoryApi:
    properties:
      id:
        type: integer
      name:
        type: string
    type: object
  pkg.NewsApi:
    properties:
      category:
        $ref: '#/definitions/pkg.CategoryApi'
      category_id:
        type: integer
      content:
        type: string
      created_at:
        type: string
      id:
        type: integer
      image_url:
        type: string
      read_time:
        type: integer
      share_count:
        type: integer
      title:
        type: string
      updated_at:
        type: string
      user:
        $ref: '#/definitions/pkg.User'
      user_id:
        type: integer
      views:
        type: integer
    type: object
  pkg.User:
    properties:
      about:
        type: string
      avatar_url:
        type: string
      created_at:
        type: string
      id:
        type: integer
      name:
        type: string
      updated_at:
        type: string
    type: object
host: localhost:8080
info:
  contact: {}
  description: This is a sample RESTful API server.
  title: Blueprint API
  version: "1.0"
paths:
  /news:
    get:
      consumes:
      - application/json
      description: Returns a paginated list of news items.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/pkg.NewsApi'
            type: array
      summary: Get news list
      tags:
      - news
swagger: "2.0"
